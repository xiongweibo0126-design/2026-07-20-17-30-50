# 2026-07-20

## TokenBase — English token-service landing site
- Built a static marketing site (index.html + styles.css + script.js) in the workspace root.
- Goal: English-facing clone/inspiration of tokenapibay.com, selling cheap AI tokens to foreign developers.
- Brand: **TokenBase**; value prop = "up to 90% off" GPT-4o/Claude/DeepSeek, OpenAI+Anthropic compatible, $5 free credit, no card.
- Sections: announce bar, nav, hero (code card + floating stats), compatibility strip, features (4), pricing/savings cards (4), 3-step quickstart, docs/code example w/ copy button, model library chips, enterprise (3), FAQ accordion, final CTA, footer.
- Design: light theme, indigo brand (#4f46e5→#7c3aed) + savings green (#10b981), Inter + JetBrains Mono, responsive (mobile burger nav).
- Preview: `python -m http.server 8080` in workspace root → http://127.0.0.1:8080
- Note: pricing/features are illustrative placeholders — user should swap in real rates, real domain, and legal copy before launch.

## TokenBase MVP — backend + payments (same project, server/ subdir)
- User wants to go live AND accept payments (foreign customers). Chose **Paddle/LemonSqueezy (MoR)** + **full MVP**.
- Built zero-dependency Node backend (server/server.js) + JSON store (server/store.js). No npm install needed → runs anywhere.
- `server/config.js` = editable prices/packages (user sets prices here: `pricing.models` per-token, `packages` top-up tiers, `freeCredit`, `upstream` keys).
- Endpoints: `/api/register` (email→sk- key, +free credit), `/api/me`, `/api/checkout` (creates Paddle/LemonSqueezy checkout or sandbox demo URL), `/api/paddle-webhook` + `/api/ls-webhook` (HMAC verify, credits balance), `/v1/chat/completions` + `/v1/messages` (proxy to upstream w/ per-token billing), `/v1/models`, `/v1/balance`.
- Payment: real mode needs PADDLE_VENDOR_ID/API_KEY/CLIENT_TOKEN/SECRET/WEBHOOK_SECRET (or LS keys) in env; **SANDBOX_DEMO=true** lets the whole loop run locally with no merchant account (checkout URL credits balance immediately).
- Upstream proxy is in **demo mode** by default (returns synthetic completion, deducts ~real token cost) until `config.upstream` keys are filled → then forwards to OpenAI/Anthropic/OpenRouter.
- Dashboard: `server/public/dashboard.html` (+ dashboard.js) — register, view key, balance, buy credits, copy endpoint snippet. Landing CTAs (index.html) now point to `/dashboard`.
- Verified full loop locally on port 3100: register→$5 credit→proxy call→balance 5→4.99995→checkout (pro $20)→sandbox pay→balance ~25.
- `server/DEPLOY.md` = deploy steps (Vercel/Netlify/Railway + supabase/neon DB), env vars, MoR setup, legal/compliance warnings (reselling OpenAI/Anthropic may violate ToS — use wholesale upstream + multi-vendor failover).
- IMPORTANT caveat to relay: this is an MVP skeleton. Before real money, wire a real DB (not JSON store), real upstream keys, real MoR webhooks, and get tax/ToS advice.

## Pivot: domestic LLMs ONLY (no OpenAI/Anthropic resale)
- User clarified: do NOT resell OpenAI/Anthropic; only resell **domestic Chinese LLM APIs** (合规压力更小，各厂商有官方 API/合作机制).
- Restructured `server/config.js`: added `models` catalogue (12 models: deepseek-chat/reasoner, qwen-max/plus/turbo, glm-4-plus/air, moonshot-v1-8k/Kimi, ernie-4.0-8k, abab6.5s/MiniMax, step-1v-mini, doubao-pro) and `providers` map (deepseek/qwen/zhipu/moonshot/baidu/minimax/stepfun/doubao) each with OpenAI-compatible baseUrl + env API key. Removed single UPSTREAM_BASE_URL/KEY.
- `server/server.js`: `resolveProvider(model)` routes each call to its provider; strips `/v1` from path and appends `/chat/completions`; rewrites `model` to `upstreamModel` when needed; per-provider DEMO fallback (provider with no key => synthetic response + real billing). /v1/models now returns the 12-model catalogue.
- Landing `index.html` fully reworded to domestic models: title/desc, announce, hero (deepseek-chat example, $0.14/1M), compatibility strip (OpenAI/LangChain/Dify/LiteLLM/FastGPT), 4 pricing cards (DeepSeek V3 / Qwen-Max / GLM-4-Plus / Kimi), model chips, FAQ, footer disclaimer ("not affiliated, tokens resold via official API channels").
- `server/DEPLOY.md` updated: provider env keys, compliance note now about domestic-only resale via official platforms/partner programs.
- Verified on port 3100: register->$5, /v1/models=12 domestic models, chat deepseek-chat & qwen-max route+bill correctly, checkout+sandbox top-up works.
- Note: prices in config.js are PLACEHOLDERS — user sets margins. Real upstream still needs provider API keys (currently all DEMO).

## Deploy prep for non-technical user (foolproof)
- User is a self-described "技术小白" (non-coder); wants the site live, step-by-step guidance.
- Added root deploy files so the whole repo deploys as one Node app: `package.json` (start=node server/server.js, node>=18), `Procfile` (web: node server/server.js), `railway.json` (nixpacks builder, healthcheck /), `.env.example` (all env vars, commented, Chinese), `.gitignore` (data/, node_modules/, .env).
- Recommended path for newbie: GitHub Desktop (GUI, no CLI) to push repo → Railway "Deploy from GitHub" (free, auto URL). Sandbox payment + demo upstream work with ZERO env vars, so site can go live immediately; keys are optional upgrades.
- Root `DEPLOY.md` rewritten as a pure click-through Chinese newbie guide (GitHub Desktop → Railway → fill keys → domain → caveats).
- Verified `node server/server.js` boots and serves / (200), /dashboard (200), /api/packages (4) — confirms Railway start command works.
- Caveat to relay: JSON store resets on restart (swap to Supabase/Neon for production); user sets prices in config.js; compliance = domestic-only resale is lower risk but advise legal check at scale.

## Switched deploy target: Railway -> Render.com
- User (non-coder) struggled with Railway: GitHub not connected, "service domain limit" errors from too many test services. Chose Render.com instead (cleaner UI, auto URL, no domain-limit headaches).
- Updated root DEPLOY.md to use Render as the primary platform (render.com → New Web Service → connect xiongweibo0126/design → Build `npm install`, Start `node server/server.js`, Free tier). Kept env-var table + caveats.
- Added `render.yaml` (Blueprint one-click: web service, node, free, startCommand node server/server.js).
- User's GitHub repo is named `design` (xiongweibo0126/design), made public. Railway `railway.json`/`Procfile` left in repo (harmless, Render ignores them).
- NOTE: user in China; Render may need a credit card for free-tier verification. If blocked, fallback = Netlify (static only, no backend) or keep trying Railway after deleting old services.
- Next: walk user through Render Web Service creation; verify live URL returns site + can register/claim $5.

## Paddle integration setup (user registered Paddle account)
- 3 one-time products created in Paddle: Starter $5, Pro $20, Scale $100 (prices match config.packages).
- Starter Price ID: `pri_01ky0032fkjw8c1ycp28mzh39z`. STILL NEED Pro & Scale Price IDs from user.
- Config (server/config.js) reads all Paddle values from ENV (no code change needed for keys): PAYMENT_PROVIDER=paddle, PADDLE_API_KEY, PADDLE_WEBHOOK_SECRET, PADDLE_ENV=production (Live keys!), PADDLE_PRICE_STARTER/PRO/SCALE.
- Paddle webhook URL to configure in Paddle: `https://<site>.onrender.com/api/webhook/paddle`; subscribe to `transaction.completed`.

## CRITICAL: deploy source mismatch (must remember)
- Local working folder's git remote = `xiongweibo0126-design/2026-07-20-17-30-50` (WorkBuddy-managed), but Render deploys `xiongweibo0126/design` (PRIVATE, raw 404). 
- Edits made in this WorkBuddy folder do NOT auto-sync to the live site. To ship a fix, must copy `server/server.js` + `server/config.js` into the `design` repo's local folder (via GitHub Desktop) and push.
- FIXED Paddle bug: verifyPaddle split sig on `,` but Paddle sends `ts=...;h1=...`. Rewrote to split `[;,]`; verified locally transaction.completed webhook credits balance (5→10). Also removed unused `business` package (only 3 Paddle products). Committed locally (not pushed; can't push to design without user).

## RESOLUTION: real deploy source + push done
- CORRECTION: the GitHub account is actually `xiongweibo0126-design` (with `-design`), NOT `xiongweibo0126`. The repo `xiongweibo0126/design` does NOT exist (404). The only owned repo is `xiongweibo0126-design/2026-07-20-17-30-50` — which IS this WorkBuddy folder's git remote AND what Render deploys (live site `tokenbase-814i.onrender.com`).
- User gave a GitHub PAT (`ghp_...`, classic, repo scope). Used it to push `main` → `bf30171..67840bb`. Fix is now on GitHub; Render auto-redeploys (1–3 min). Told user to DELETE the PAT after push.
- After push, live site still showed old code briefly (packages API returned `business $500`); Render rebuild in progress. Verify new code live when `business $500` package disappears from /api/packages.
- Paddle fully configured: API key (live), webhook secret, webhook URL active, 3 products (Starter/Pro/Scale price IDs known). PAYMENT_PROVIDER=paddle + all PADDLE_* env vars set in Render. DeepSeek API key also set in Render.
- TODO after redeploy: test a real $5 purchase → confirm balance +$5. Then advise swapping JSON store → Supabase/Neon before scaling, and filling real model prices in config.js.

## 2026-07-21 (evening) — Paddle checkout fix + onboarding
- FIXED Paddle `invalid_url` error: old code used `POST /checkout/sessions` (deprecated). Rewrote `paddleCheckout()` to use Paddle Billing v2 `POST /transactions`, reads checkout URL from `j.data.checkout.url` (was `j.data.url`). Pushed → commit f5f1347.
- After fix, error changed to `transaction_checkout_not_enabled` = Paddle account onboarding not yet complete (NOT a code issue). User must finish Paddle verification/onboarding to enable checkout.
- Created legal pages for Paddle onboarding: `privacy.html`, `refunds.html` (terms.html already existed). Added server routes `/terms` `/privacy` `/refunds`. Pushed → commit ad67213.
- Paddle onboarding form URLs to give: domain `tokenbase-814i.onrender.com`, terms/privacy/refunds at `/terms` `/privacy` `/refunds`, pricing page `/dashboard`.
- Product description used for Paddle: "AI API gateway and token reselling platform... prepaid credit... per-token billing... digital SaaS".
- New GitHub PAT used for pushes (user to delete after). Account: xiongweibo0126-design/2026-07-20-17-30-50.
- NEXT: user completing Paddle compliance questionnaire → then business details + bank account + ID verification → wait for Paddle approval → checkout auto-enables (no code change needed).


## 2026-07-21 (night) — Paddle API key permission fix
- Paddle onboarding fully completed: Step 02 Verify account = green. Payout settings saved (China, Individual, Payoneer xiongweibo0125@gmail.com). Step 01 set up live account completed.
- BUG: forbidden / not authorized to create|read transaction when POST /transactions. Root cause: editing an EXISTING API key's permissions does NOT take effect on Paddle — must CREATE A NEW KEY with transactions read+write (or Select all) at creation time.
- Verified live: new key pdl_live_apikey_01ky2degt86jyqzxm2k4hr3mf0_... returns HTTP 201 with checkout.url. Old two keys (01kxzzng..., 01ky2czg...) are dead/under-permissioned.
- NEXT: user updates Render env PADDLE_API_KEY to new key, redeploy, test real $5 payment on /dashboard.
- Note: default payment link returns to /dashboard?_ptxn=... (Paddle default payment link set to dashboard URL).


## 2026-07-21 (night) — root cause of "jumps back to register"
- DIAGNOSIS: Paddle integration code is CORRECT (paddleCheckout passes custom_data {userId,packageId}; webhook credits user by id). The recurring "Invalid API key / jumps to register" is purely the JSON-file store (store.js data.json) being wiped by Render's ephemeral FS: free-tier cold-starts (after idle) and every redeploy reset the file, so registered users vanish.
- CONCLUSION: JSON store cannot work on Render free tier. Must migrate to a real DB (Supabase/Neon Postgres via DATABASE_URL) to make registration + payment reliable. User agreed to do the DB migration now (had asked "is switching to Supabase/Neon enough" -> yes).
- PLAN: add `pg` dep, rewrite store.js to use Postgres when DATABASE_URL is set (keep JSON fallback when unset for local dev). Make store fns async. Create tables on startup (CREATE TABLE IF NOT EXISTS). Set DATABASE_URL on Render. Then re-test payment end-to-end.

## 2026-07-22 (early AM) — Paddle.js overlay integration (fix 'pops back')
- ROOT CAUSE of 'click Buy and it pops back to dashboard': Paddle Billing v2 `checkout.url` returns YOUR domain + `_ptxn=txn_...` param (NOT a paddle.com redirect). It's designed for Paddle.js to detect `_ptxn` and open an OVERLAY checkout. Old code did `window.location.href = d.url` which just reloaded own page with an unrecognized query param -> silent no-op.
- FIX: integrated Paddle.js overlay checkout.
  - dashboard.html: load https://cdn.paddle.com/paddle/v2/paddle.js
  - dashboard.js: initPaddle() fetches /api/paddle-config (env + clientToken), Paddle.Initialize; buy() extracts txn id from `_ptxn=` and calls Paddle.Checkout.open({transaction}); also auto-open on page load if `_ptxn` present; show success msg on checkout.completed.
  - server.js: new GET /api/paddle-config returns {environment, token: paddle.clientToken}
  - config.js: paddle.clientToken from PADDLE_CLIENT_TOKEN env
  - .env.example: documented PADDLE_CLIENT_TOKEN
- Pushed -> commit 1eb6a3f. User provided new GitHub PAT (ghp_YpB...) for push.
- REMAINING: user must create Paddle CLIENT-SIDE TOKEN (not API key) in Paddle Dashboard -> Developer tools -> Authentication -> Client-side tokens, then set PADDLE_CLIENT_TOKEN on Render. Also ensure DATABASE_URL env set on Render.
- VERIFIED live before fix: /api/register (balance 5), /api/packages (3 pkgs), /api/checkout (returns _ptxn url) all work -> DB + Paddle API healthy; only missing Paddle.js client token.
