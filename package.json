{
  "name": "tokenbase",
  "version": "1.0.0",
  "private": true,
  "description": "TokenBase — cheap domestic LLM API gateway (OpenAI-compatible)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
